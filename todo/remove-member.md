# TODO: Remove Member and Leave Group

- [ ] Fetch current user info to determine roles.
- [ ] Implement `handleRemoveMember(userId)` in `app/group-detail/[id].tsx`.
- [ ] Add `TouchableOpacity` or `ActionSheet` logic to member items (Admin only).
- [ ] Update `handleLeaveGroup` to use dynamic `userId`.
- [ ] Add loading state during removal operation.
- [ ] Refresh member list upon successful removal.
- [ ] Show success toast/alert.
